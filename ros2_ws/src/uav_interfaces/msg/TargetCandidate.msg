int32 candidate_index
string target_label
float32 confidence

# Bounding box coordinates normalized to the image width and height in the range [0.0, 1.0].
float32 bbox_x_min_norm
float32 bbox_y_min_norm
float32 bbox_x_max_norm
float32 bbox_y_max_norm

# Visual near/far estimate retained for the existing front-camera feedback baseline.
string distance_bucket
